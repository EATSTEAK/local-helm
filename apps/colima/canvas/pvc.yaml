apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: canvas-data
  namespace: default
  labels:
    app: canvas
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: local-path
  volumeMode: Filesystem
