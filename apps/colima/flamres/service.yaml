apiVersion: v1
kind: Service
metadata:
  name: flamres
  namespace: flamres
spec:
  type: ClusterIP
  selector:
    app: flamres
  ports:
    - name: http
      port: 3000
      targetPort: http
      protocol: TCP
