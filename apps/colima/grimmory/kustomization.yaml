apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: default
generatorOptions:
  disableNameSuffixHash: true
configMapGenerator:
  - name: grimmory-values
    files:
      - values.yaml=values.yaml
resources:
  - helmrelease.yaml
